import { Engine } from '../elements/engine.js';

Engine.plugins.add({
    load: engine => {
        const { } = engine;

        // sx: 0,
        // sy: 0,
        // sw: source.width,
        // sh: source.height,
        // dx: 100,
        // dy: 100,
        // dw: source.width,
        // dh: source.height
    },
    unload: engine => {
    }
});