export const initializeNetwork = async (api, options) => {
    const { systems } = api;
    const { } = options;

    systems.push({
        validate: entity => {
        },
        add: entity => {
        },
        remove: entity => {
        }
    });
};