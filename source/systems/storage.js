export const initializeStorage = async (api, options) => {
    const { systems } = api;
    const { } = options;

    systems.push({
        update: (delta, elapsed) => {
        },
        validate: entity => {
        },
        add: entity => {
        },
        remove: entity => {
        }
    });
};