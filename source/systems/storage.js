export const createStorageSystem = async (api, options, createEntity, deleteEntity) => {
    const { } = api;
    const { } = options;

    return {
        update: (delta, elapsed) => {
        },
        validate: entity => {
        },
        add: entity => {
        },
        replace: entity => {
        },
        remove: entity => {
        }
    }
};