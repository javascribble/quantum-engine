export const initializeStorage = async (api, options) => {
    const { systems } = api;
    const { } = options;

    systems.push({
        validate: entity => {
        },
        add: entity => {
        },
        remove: entity => {
        }
    });
};