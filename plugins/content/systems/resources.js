const defaultResourcesOptions = {
};

export const createResourcesSystem = async (resourcesOptions) => {
    const options = {
        ...defaultResourcesOptions,
        ...resourcesOptions
    };

    return {};
};