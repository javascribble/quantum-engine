import { load } from '../../../engine/main';

export const parseGltf = async (resource) => {
    return resource;
};

// const loadSceneResources = async (resources) => {
//     resources.passes = await load(resources.passes);
//     resources.shaders = await load(resources.shaders);
//     resources.layouts = await load(resources.layouts, loadLayout);
//     resources.programs = await load(resources.programs, loadProgram);
//     resources.buffers = await load(resources.buffers);
//     resources.textures = await load(resources.textures, loadTexture);
// };
