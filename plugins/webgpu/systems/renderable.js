export const createRenderableSystem = (options) => {
    return {
        components: [meshComponent, materialComponent],
        add(entity) {
        },
        delete(entity) {
        },
        update(deltaTime) {
        }
    }
};
