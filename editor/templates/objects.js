import { template } from '@javascribble/quantum';

//export const objectsIcons = repeat(icons, [editingIcons]);

export const objects = template(`
<style>
</style>
<div class="menu"></div>
<tree-component></tree-component>
`);